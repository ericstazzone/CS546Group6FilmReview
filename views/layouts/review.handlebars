<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.0/font/bootstrap-icons.css">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-select@1.13.14/dist/css/bootstrap-select.min.css">
        <link rel="stylesheet" href="/public/css/styles.css">
        <title>{{title}}</title>
    </head>
    <body>
        <div class="container home-container">
            <div class="row raise">
                <div class="col-md-12">
                    <h1>{{title}}</h1>
                    <div class="row">
                        <div class="col-md-12">
                            <div class="card">
                                <div class="card-body">
                                    <h5 class="card-title">{{content}}</h5>
                                    <p class="card-text"><span class="text-muted">Rating: {{rating}}</span></p>
                                    <p class="card-text"><span class="text-muted">Created on {{createdDate}}</span></p>
                                    <p class="card-text"><span class="text-muted">Movie ID: {{movieId}}</span></p>
                                    <p class="card-text"><span class="text-muted">User ID: {{userId}}</span></p>
                                    <p class="card-text"><span class="text-muted">Page Hits: {{counter}}</span></p>
                                </div>
                            </div>
                        </div>
                    </div>
                    {{#if comments}}
                        <div class="row">
                            {{!-- If there are comments then display them --}}
                            {{#each this}}
                                <p>{{this.userId}}</p>
                                <p>{{this.name}}</p>
                                <p>{{this.createdDate}}</p>
                                <p>{{this.commentContent}}</p>
                            {{/each}}
                        </div>
                    {{else}}
                        <p>No comments yet</p>
                    {{/if}}
                    <form method="POST" action="/comments" id="commentForm">
                        <label for="commentForm">Comment</label>
                        <input type="text" placeholder="Comment" name="commentForm" id="commentForm"/>
                        <button class="btn btn-primary" type="submit">Submit</button>
                    </form>
                    <a href="/">Home</a>
                </div>
            </div>
        </div>
    </body>
</html>